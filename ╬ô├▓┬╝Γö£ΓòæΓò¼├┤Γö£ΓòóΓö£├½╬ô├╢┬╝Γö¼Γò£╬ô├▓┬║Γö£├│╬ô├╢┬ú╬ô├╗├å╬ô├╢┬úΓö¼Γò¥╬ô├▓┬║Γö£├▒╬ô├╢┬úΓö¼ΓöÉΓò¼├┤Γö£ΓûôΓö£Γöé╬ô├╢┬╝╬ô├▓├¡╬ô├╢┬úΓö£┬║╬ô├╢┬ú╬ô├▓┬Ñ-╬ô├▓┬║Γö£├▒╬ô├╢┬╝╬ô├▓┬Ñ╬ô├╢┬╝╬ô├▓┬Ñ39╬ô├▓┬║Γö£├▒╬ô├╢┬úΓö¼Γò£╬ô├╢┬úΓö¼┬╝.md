# 酷9反编译修复 - 第39版

本版基于 0818酷9-继续修复38.zip。

## 本轮实际修复

文件：`app/src/main/java/com/github/tvbox/osc/ui/activity/LivePlayActivity.java`

修复直播 EPG 查询中的一个明确的 JADX 控制流错误：

原代码：
`if (strArrM335 != null || C0016.m622(strArrM335[1]))`

该条件在 `m335()` 返回 null 时仍会访问 `strArrM335[1]`，会造成 NullPointerException，直接中断直播 EPG 加载。

改为安全的存在性/长度判断：
`strArrM335 == null || strArrM335.length < 2 || C0016.m622(strArrM335[1])`

只有存在有效的第二项时才使用频道别名，否则继续使用原频道名称。

## 对照依据

TVBox 的直播入口仍是 `LivePlayActivity`，直播数据包括频道组、频道、EPG 等结构。本次只修酷9反编译代码中的明确逻辑错误，不覆盖酷9自己的 UI。
