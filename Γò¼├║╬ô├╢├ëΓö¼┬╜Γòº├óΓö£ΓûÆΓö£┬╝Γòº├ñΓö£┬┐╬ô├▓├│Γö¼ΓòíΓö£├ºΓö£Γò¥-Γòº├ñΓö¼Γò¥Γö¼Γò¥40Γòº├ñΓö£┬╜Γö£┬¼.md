# 酷9反编译修复 - 第40版

基于第39版继续。

本轮重点：LivePlayActivity 的 EPG 数据缓存逻辑。

修复：
- 将 `strM125 != null && cache.get(key) == null` 修正为 `strM125 == null && cache.get(key) == null`。
- 原条件在已有缓存值时反而进入 SQLite 查询，属于明显的反编译控制流/条件还原错误；正确逻辑应在 LruCache 未命中时查询持久化缓存。
- 保留现有酷9 UI、播放器和 TVBox 直播结构，不覆盖 LivePlayActivity。

参考：TVBoxOS 当前公开工程仍包含完整 LivePlayActivity 与直播相关模型/Adapter；公开工程结构可用于母版逻辑核对。
