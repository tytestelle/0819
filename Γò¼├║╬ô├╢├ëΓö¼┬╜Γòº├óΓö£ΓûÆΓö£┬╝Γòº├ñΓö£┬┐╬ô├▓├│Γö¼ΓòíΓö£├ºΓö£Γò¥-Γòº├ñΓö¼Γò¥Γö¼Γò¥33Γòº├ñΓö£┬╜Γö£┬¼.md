# 酷9反编译工程修复 - 第33版

本版基于第32版继续。

## 本轮实际修复

1. Guava Future 的 `get(long, TimeUnit)`
   - 恢复超时等待、线程中断、等待节点入队/出队以及完成结果返回。
   - 不再直接返回 null。

2. Cling `RecoveringUDA10DeviceDescriptorBinderImpl.describe()`
   - 恢复 descriptor 正常解析。
   - 失败后依次尝试修复前导垃圾字符、尾部垃圾字符、缺失 namespace，并重新解析。
   - 多次修复失败后保留原异常路径。

## 当前状态

JADX `Method dump skipped` 数量进一步减少；剩余主要集中在腾讯 X5、JNA、ConstraintLayout MotionEffect/Flow 等第三方库。

本轮没有替换酷9 LivePlayActivity，也没有覆盖酷9资源。
