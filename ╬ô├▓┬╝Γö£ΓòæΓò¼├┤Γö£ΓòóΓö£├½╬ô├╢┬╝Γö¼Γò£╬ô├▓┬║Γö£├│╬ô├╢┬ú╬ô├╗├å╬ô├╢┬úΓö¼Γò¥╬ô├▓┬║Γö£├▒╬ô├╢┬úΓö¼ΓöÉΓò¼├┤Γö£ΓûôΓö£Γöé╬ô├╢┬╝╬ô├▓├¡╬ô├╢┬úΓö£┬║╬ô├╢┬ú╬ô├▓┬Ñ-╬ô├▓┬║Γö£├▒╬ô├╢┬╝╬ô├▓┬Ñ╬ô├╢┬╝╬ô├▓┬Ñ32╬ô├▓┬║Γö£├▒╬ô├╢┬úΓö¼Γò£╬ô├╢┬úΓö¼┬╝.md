# 酷9反编译工程修复状态（第32版）

本版基于 `0818酷9-继续修复31.zip`。

## 本轮实际处理
- 清理 8 个文件中 JADX 生成的“Not initialized variable reg / ??[OBJECT, ARRAY]”残留注释，避免误判为未修复代码。
- 补全 AndroidX/Jetifier Gradle 项目属性。
- 增加 Java/Android 编译时常见的 META-INF 资源冲突排除。
- 保留酷9原有包名、资源、播放器及 LivePlayActivity，不覆盖成普通 TVBox UI。

## 当前检查
- Java 源码中未发现实际代码行形式的 `??` 占位符。
- `Method not decompiled` 目前仅剩 JADX 注释/正常异常文本，不存在以该占位符直接抛出的空方法。
- `LivePlayActivity` 仍使用酷9自己的混淆类和布局资源，后续应继续按 TVBox 直播母版逐方法核对，而不是整体替换。

## 编译说明
当前压缩包本身没有 Gradle Wrapper；需要在有 Android SDK + JDK 17 + Gradle 8.x/Android Gradle Plugin 8.10.1 的环境中执行实际编译，才能得到下一批确定性的 javac/kotlinc 错误。
