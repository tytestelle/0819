第42版修复记录

本版继续以酷9反编译代码为主体，TVBox源码仅作为母版逻辑参照，不覆盖酷9二次开发代码。

LivePlayActivity：
1. EPG缓存查询：strM125 已经从 LruCache 取出后，原反编译条件再次查询同一个 LruCache，属于重复且可能改变语义的条件。改为仅在 strM125 == null 时进入数据库查询分支。
2. 保留第41版的频道别名/EPG安全判断，不替换酷9自己的UI、播放器或业务类。

本版仍未宣称已完整编译APK；后续继续以实际编译错误和酷9自身调用关系为依据修复。
