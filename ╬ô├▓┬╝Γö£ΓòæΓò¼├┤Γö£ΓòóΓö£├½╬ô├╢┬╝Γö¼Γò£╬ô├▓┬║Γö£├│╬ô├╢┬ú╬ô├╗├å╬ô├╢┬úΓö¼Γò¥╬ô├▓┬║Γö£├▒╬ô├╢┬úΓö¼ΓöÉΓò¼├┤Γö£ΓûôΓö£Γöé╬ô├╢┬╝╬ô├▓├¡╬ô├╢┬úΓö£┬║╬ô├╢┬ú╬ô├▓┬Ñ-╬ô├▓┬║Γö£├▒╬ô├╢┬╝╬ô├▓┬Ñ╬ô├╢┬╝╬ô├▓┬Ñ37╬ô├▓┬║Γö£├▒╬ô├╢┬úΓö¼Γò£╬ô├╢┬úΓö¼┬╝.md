第37版修复记录

本版基于 0818酷9-继续修复36.zip。

1. 恢复 com.tencent.smtt.sdk.k 的 b(boolean,boolean)：
   - HTTP GET
   - Range 断点续传
   - 301/302/303/307/308 重定向
   - 200/206 响应
   - x5.tbs.temp 写入
   - 下载完成后原子替换 x5.tbs
   - 空间检查、HTTP/异常记录、暂停/取消检查

2. 恢复 com.tencent.smtt.sdk.k 的 a(boolean,boolean)：
   - 从 TbsDownloadConfig 读取下载 URL 列表
   - 兼容 JSON 数组和分隔字符串
   - 主 URL 优先
   - 多 URL 失败切换与重试
   - 成功后进入原有 c(false) 完成处理
   - 最终统一关闭 HTTP 请求并保存下载状态

说明：这两个方法原始 TBS SDK 属于闭源代码，因此本版是依据当前反编译类已有字段、调用关系和 TBS 下载配置结构恢复的功能等价实现，不声称逐指令还原原厂字节码。
