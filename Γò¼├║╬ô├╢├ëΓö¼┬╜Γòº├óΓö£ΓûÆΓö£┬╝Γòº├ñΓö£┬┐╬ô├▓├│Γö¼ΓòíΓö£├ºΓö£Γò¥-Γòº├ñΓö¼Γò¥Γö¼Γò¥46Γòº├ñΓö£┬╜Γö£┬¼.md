# 酷9修复第46版

本版继续以酷9反编译代码为主体，TVBox仅作为母版参照。

## 本轮结论

对第45版进行了静态检查。LivePlayActivity 中此前修复的：
- EPG频道别名空数组/空值保护
- EPG缓存命中判断
- 备用EPG地址为空保护
- 两条备用地址处理路径一致性

均保留。

本轮没有发现能够在“不改变酷9原有业务语义”的前提下安全替换的第二处控制流错误，因此没有为了制造版本号而硬改业务代码。

## 发现的真正构建问题

当前工程没有 gradlew / gradle-wrapper.jar，因此不能在当前资料中可靠执行 Gradle 编译。
同时 app/build.gradle 只有少量显式依赖，而源码包含 Media3、Cling、Leanback、ConstraintLayout、腾讯X5、OkHttp、Doikki、JNA、Kotlin/协程等大量第三方代码/引用。直接猜版本并塞依赖会破坏酷9原有版本关系。

因此本版增加了 `BUILD_诊断.bat`，用于在补齐对应 Gradle 环境后执行构建并把第一批真实错误保存下来。

## 原则

1. 不用最新TVBox覆盖酷9。
2. 不改变酷9 res/UI。
3. TVBox只用于确认缺失方法/原始算法。
4. 对没有足够证据的反编译控制流不猜测修改。
