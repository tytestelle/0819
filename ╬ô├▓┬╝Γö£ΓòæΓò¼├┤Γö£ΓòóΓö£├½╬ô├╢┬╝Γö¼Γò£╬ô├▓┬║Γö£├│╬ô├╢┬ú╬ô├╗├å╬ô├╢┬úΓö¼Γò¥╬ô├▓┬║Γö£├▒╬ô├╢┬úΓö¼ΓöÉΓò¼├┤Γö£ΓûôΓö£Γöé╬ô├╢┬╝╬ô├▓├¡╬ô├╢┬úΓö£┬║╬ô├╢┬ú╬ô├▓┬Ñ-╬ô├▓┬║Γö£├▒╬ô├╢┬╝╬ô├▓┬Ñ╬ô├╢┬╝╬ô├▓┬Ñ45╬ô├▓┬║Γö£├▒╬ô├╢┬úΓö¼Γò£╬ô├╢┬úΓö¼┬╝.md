# 酷9反编译修复 - 第45版

本版继续以酷9反编译工程为主体，TVBox仅用于交叉确认原始设计意图，不覆盖酷9业务/UI。

## 本轮修改

继续检查 `LivePlayActivity` 的 EPG 备用地址处理。发现同一套备用地址逻辑在异常/数据库回退路径中还有一份重复的地址循环：

- `m355(addressItem)` 返回 `null` 后仍然调用 `m361(addressItem)`；
- 与前面的正常路径保护不一致；
- 在异常回退路径同样增加 `null` 保护，直接进入下一项；
- 非空地址完全保持酷9原有处理流程。

## 原则

没有用 TVBox 的 `LivePlayActivity` 整段替换酷9。TVBox只作为母版参考，用于判断反编译后的控制流是否合理。

## 下一步

继续检查酷9自己的频道切换、EPG缓存、备用源和播放器切源路径，重点寻找“代码存在但 JADX 控制流还原错误”的位置；在具备 Android SDK/Gradle 环境后再进行实际 APK 编译验证。
