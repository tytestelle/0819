# 酷9反编译修复状态 - 第38版

本版基于第37版继续。

## 本轮实际修复

### LivePlayActivity.onDestroy()

发现酷9自己的 `LivePlayActivity` 中存在一个明确的 JADX 错误：

```java
case 47384:
    m498(...);
    throw null;
```

该分支位于 DLNA/DMR 服务清理流程。`throw null` 会导致检测到 DMR 服务时退出直播页面立即崩溃，而且后面的播放器、ZLive、服务对象清理代码永远不会执行。

已恢复为继续进入后续清理分支：

```java
m498(...);
i5 = 47417;
break;
```

## 当前检查

- `com.github.tvbox` 下的 `Method not decompiled`：0
- `com.github.tvbox` 下明确的 `throw null`：0
- 本轮没有覆盖酷9原有 res/UI/播放器。

## 构建说明

当前容器没有可用 Android SDK/Gradle Wrapper，无法在这里宣称已经完成 APK 编译；后续继续以实际编译错误和运行链路为准。
